# وثيقة نظام سوقك

**الإصدار:** 1.0  
**تاريخ الوثيقة:** 2026-09-09  
**حالة الوثيقة:** مرجع فني وتشغيلي مبني على الكود الحالي

## 1. الغرض من الوثيقة

سوقك منصة سوق إلكترونية متعددة الأدوار، تتيح نشر الإعلانات والبحث عنها والتواصل حولها، مع أدوات للبائعين والإدارة، ومحفظة وطلبات شراء ووساطة آمنة وبرنامج تسويق وإحالات.

هذه الوثيقة مرجع موحد لـ:

- بنية المشروع والتقنيات المستخدمة.
- جميع صفحات الواجهة ومساراتها وصلاحيات الوصول إليها.
- الأنظمة الوظيفية وتدفقات العمل الأساسية.
- مجموعات API والكيانات المخزنة في MongoDB.
- التشغيل المحلي والنشر والمراقبة والاختبارات.
- حدود النظام الحالية والنقاط التي تحتاج إلى توثيق أو تطوير لاحق.

> مصدر الحقيقة لهذه الوثيقة هو الكود الموجود في `frontend/src` و`backend/src`. عند اختلاف هذه الوثيقة مع التنفيذ، يعتمد التنفيذ الحالي إلى أن يتم تحديث الوثيقة.

## 2. ملخص المنتج

### الأدوار

| الدور | الصلاحيات الرئيسية |
|---|---|
| زائر | تصفح الصفحة الرئيسية والفئات والإعلانات والملفات العامة والصفحات القانونية والتقييمات العامة. |
| مستخدم / مشتري | الحساب، المفضلة، المتابعة، الرسائل، الإشعارات، الطلبات، المحفظة، التقييمات، البلاغات والإحالات. |
| بائع | كل صلاحيات المستخدم، إضافة وتعديل وإدارة الإعلانات، استقبال الرسائل، الاشتراكات، التحقق، العمولات، الإعلانات المميزة والتحليلات. |
| مدير | إدارة كاملة للمستخدمين والإعلانات والمحتوى والموقع والمالية والدعم والإعدادات والسجلات والصحة التشغيلية. |
| وسيط | وظائف الوساطة عند تفعيل النظام، مثل الحملات والعضويات والصفقات والأدلة والتقييمات والإنجازات. يتم تمثيله عمليًا من خلال المستخدم وملف الوسيط. |

### قواعد عمل مهمة

1. الإعلان الجديد يبدأ عادة بالحالة `pending` ولا يظهر للعامة إلا بعد اعتماده.
2. الإعلانات المعتمدة فقط هي القابلة للظهور العام، مع استثناءات خاصة بلوحات الإدارة والمالك.
3. الوصول إلى صفحات الحساب يتطلب جلسة دخول.
4. الوصول إلى صفحات الإدارة يتطلب الدور `admin`.
5. نظام الوساطة لا يعمل إلا عند تفعيله من إعدادات النظام.
6. الحذف يدعم الحذف المنطقي والاستعادة من سلة المحذوفات في عدة كيانات.

## 3. البنية التقنية

### الواجهة الأمامية

- React 18 مع Vite.
- React Router للمسارات والحماية.
- Axios للاتصال بالـ API.
- TanStack React Query لإدارة جلب البيانات عند استخدامه في الوحدات.
- Tailwind CSS وملف `frontend/src/index.css` للتنسيق.
- Framer Motion للحركة.
- Recharts للرسوم البيانية.
- Socket.IO Client للمحادثات والتحديثات الفورية.
- Web Push للإشعارات.
- React Helmet لتحسين بيانات الصفحات وSEO.
- `AuthContext` للجلسة، و`BrokerageStatusContext` لحالة نظام الوساطة، و`ChatContext` للاتصال الفوري.

### الخادم الخلفي

- Node.js بنمط ES Modules.
- Express 4.
- MongoDB 6 وMongoose 8.
- JWT للمصادقة مع refresh token.
- Socket.IO للاتصال الفوري.
- Multer وSharp لرفع ومعالجة الصور والملفات.
- Nodemailer للبريد الإلكتروني وOTP.
- Web Push لإشعارات المتصفح.
- Swagger UI على `/api-docs`.
- Sentry اختياريًا لتتبع الأخطاء.
- Winston/Morgan للسجلات.

### طبقات الخادم

```text
HTTP / Socket.IO
        |
Express middleware: CORS, security, parsing, rate limit, logging
        |
Routes -> auth/role/validation middleware
        |
Services, engines, state machines, utilities
        |
Mongoose models -> MongoDB
```

## 4. خريطة صفحات الواجهة

### 4.1 صفحات عامة

| المسار | الصفحة | الوظيفة |
|---|---|---|
| `/` | Home | الصفحة الرئيسية، البحث والتصفح والملخصات الترويجية. |
| `/categories` | Categories | عرض شجرة الفئات. |
| `/category/:slug` | CategoryPage | إعلانات فئة رئيسية. |
| `/category/:slug/:subSlug` | CategoryPage | إعلانات فئة فرعية. |
| `/search` | SearchResults | نتائج البحث والتصفية. |
| `/tag/:slug` | TagPage | الإعلانات المرتبطة بوسم. |
| `/product/:id` | ProductDetail | تفاصيل المنتج/الإعلان. |
| `/ad/:id` و`/ad/:id/:slug` | ProductDetail | مسارات بديلة متوافقة مع روابط الإعلانات. |
| `/user/:id` و`/s/:id` | SellerPublic | الملف العام للبائع وإعلاناته. |
| `/platform-reviews` | PlatformReviews | تقييمات المنصة العامة. |
| `/pricing` | Pricing | الباقات والأسعار المتاحة. |
| `/how-it-works` | HowItWorks | شرح آلية استخدام المنصة. |
| `/secure-deal-explanation` | SecureDealExplanation | شرح الصفقة الآمنة. |
| `/refund-escrow` | RefundEscrow | سياسة الاسترداد والوساطة. |
| `/terms` | Terms | الشروط والأحكام. |
| `/privacy` | Privacy | سياسة الخصوصية. |
| `/r/:code` | ReferralRedirect | تحويل رابط الإحالة وتسجيل الزيارة. |

### 4.2 الدخول والحساب

| المسار | الصفحة | الحماية |
|---|---|---|
| `/login` | Login | عامة. |
| `/register` | Register | عامة، وتدعم إنشاء حساب ومرفق التحقق عند الحاجة. |
| `/forgot-password` | ForgotPassword | عامة. |
| `/phone-forgot-password` | PhoneForgotPassword | عامة. |
| `/set-new-password` | SetNewPassword | رابط/جلسة إعادة تعيين. |
| `/verify-email` | VerifyEmail | عامة مع OTP. |
| `/account-settings` | AccountSettings | مستخدم مسجل. |
| `/notifications` | Notifications | مستخدم مسجل. |
| `/messages` | Messages | مستخدم مسجل. |
| `/chat/:productId` | Chat | مستخدم مسجل ومحادثة مرتبطة بإعلان. |
| `/favorites` | Favorites | مستخدم مسجل. |
| `/following` | Following | مستخدم مسجل. |
| `/wallet` | Wallet | مستخدم مسجل. |
| `/orders/:id` | OrderDetail | مستخدم مسجل وله صلاحية على الطلب. |

### 4.3 صفحات البائع والإعلانات

| المسار | الصفحة | الوظيفة |
|---|---|---|
| `/seller` | SellerDashboard | ملخص نشاط البائع وإعلاناته. |
| `/add-product` | AddProduct | إنشاء إعلان جديد. |
| `/choose-add-type` | ChooseAddType | اختيار نوع الإضافة. |
| `/edit-ad/:id` | EditAd | تعديل إعلان يملكه المستخدم. |
| `/my-ads` | MyAds | قائمة إعلانات المستخدم وحالاتها. |
| `/seller/featured-ad` و`/seller/feature-ad` | SellerFeaturedAd | طلب تمييز الإعلان. |
| `/seller/verification` | SellerVerification | طلب ومتابعة توثيق البائع. |
| `/seller/subscriptions` | SellerSubscriptions | الاشتراك في الباقات. |
| `/seller/commissions` | SellerCommissions | عرض العمولات وحالاتها. |
| `/commission/pay` | CommissionPay | دفع عمولة مستحقة. |
| `/referrals` | ReferralDashboard | لوحة الإحالات والعمولات التابعة للمستخدم. |

### 4.4 صفحات الوساطة

كل هذه الصفحات تتطلب تسجيل الدخول، ولا تعرض عند تعطيل الوساطة:

| المسار | الصفحة | الوظيفة |
|---|---|---|
| `/brokerage` | BrokerDashboard | لوحة الوسيط. |
| `/brokerage/campaigns` | BrokerageCampaigns | استعراض الحملات. |
| `/brokerage/campaigns/:id` | BrokerageCampaignDetails | تفاصيل حملة. |
| `/brokerage/memberships` | BrokerageMemberships | عضويات المستخدم في الحملات. |
| `/brokerage/deals` | BrokerageDeals | صفقات الوساطة. |
| `/brokerage/achievements` | BrokerageAchievements | إنجازات وشارات الوسيط. |
| `/brokerage/my-campaigns` | BrokerageSellerCampaigns | حملات البائع. |

### 4.5 صفحات الإدارة

جميع المسارات التالية تحت `/admin` ومحمية بالدور `admin`:

| المسار | الوظيفة |
|---|---|
| `/admin/dashboard` | لوحة المؤشرات الرئيسية. |
| `/admin/ads` | مراجعة وإدارة الإعلانات. |
| `/admin/sold-ads` | الإعلانات المباعة. |
| `/admin/archived-ads` | الأرشيف. |
| `/admin/deleted-ads` | الإعلانات المحذوفة. |
| `/admin/recycle-bin` | سلة المحذوفات والاستعادة والحذف النهائي. |
| `/admin/users` | إدارة المستخدمين والأدوار والحالات. |
| `/admin/deleted-users` | الحسابات المحذوفة. |
| `/admin/phone-users` | المستخدمون المسجلون بالهاتف. |
| `/admin/password-reset-requests` | طلبات إعادة كلمة المرور. |
| `/admin/categories` | الفئات وترتيبها ونقل إعلاناتها. |
| `/admin/tags` | الأوسمة. |
| `/admin/governorates` | المحافظات. |
| `/admin/cities` | المدن. |
| `/admin/reports` | البلاغات على الإعلانات والمستخدمين. |
| `/admin/plans` | الباقات والاشتراكات. |
| `/admin/bank-accounts` | الحسابات البنكية العامة للمنصة. |
| `/admin/finance-hub` | مركز الحسابات المالية، مع تبويبات الوساطة والعمولات والمحافظ والباقات. |
| `/admin/featured-requests` | طلبات تمييز الإعلانات. |
| `/admin/verification-requests` | طلبات توثيق البائعين. |
| `/admin/escrow` | لوحة الوساطة المالية. |
| `/admin/escrow-monitoring` | مراقبة عمليات الوساطة والنزاعات. |
| `/admin/support-inbox` | صندوق دعم المستخدمين. |
| `/admin/messaging` | المراسلة العامة للمستخدمين. |
| `/admin/analytics` | تحليلات المنصة. |
| `/admin/platform-reviews` | مراجعة وإدارة تقييمات المنصة. |
| `/admin/brokerage` | إدارة نظام التسويق والوساطة. |
| `/admin/referrals` | إدارة السفراء والإحالات. |
| `/admin/activity-logs` | سجل نشاطات الإدارة. |
| `/admin/audit-logs` | سجلات التدقيق. |
| `/admin/system-health` | صحة النظام والموارد والاتصال. |
| `/admin/settings` | إعدادات النظام. |
| `/admin/welcome-promotion` | إدارة الترويج الترحيبي. |

## 5. الأنظمة الوظيفية

### المصادقة وإدارة الهوية

- تسجيل بالبريد وكلمة المرور، وتسجيل/دخول بالهاتف عند تفعيل المسار.
- JWT في `Authorization: Bearer <token>`، مع refresh token وملفات تعريف الارتباط.
- التحقق من البريد عبر OTP.
- رفع صورة شخصية ووثيقة هوية.
- تغيير كلمة المرور وإعادة تعيينها.
- تبديل الدور عند السماح به.
- تعطيل الحساب أو حذفه منطقيًا.
- قفل مؤقت بعد محاولات الدخول الفاشلة.
- التحقق من حالة الجلسة عند بدء التطبيق والتحديث الدوري كل ست ساعات وعند عودة التركيز للنافذة.

### الإعلانات والسوق

- إنشاء إعلان بعنوان ووصف وسعر وعملة وفئة وموقع وصور ووسوم.
- دعم حالات المنتج: قيد المراجعة، معتمد، مرفوض، منتهٍ، مباع، مؤرشف، محذوف، محظور أو مبلغ عنه.
- البحث النصي العربي في العنوان والوصف والوسوم.
- التصفية حسب الفئة والمحافظة والمدينة والسعر والحالة.
- تحديد الموقع الجغرافي وفهرس `2dsphere`.
- بيانات حالة المنتج: جديد، مستعمل، شبه جديد، قابل للتفاوض، والسعر عند التواصل.
- إظهار الهاتف أو واتساب وفق إعدادات الإعلان.
- عداد المشاهدات والتواصل والمفضلة ونقرات الهاتف وواتساب.
- جدولة النشر والانتهاء والتذكير بالانتهاء.
- تمييز الإعلانات والترويج الترحيبي.
- وسم الإعلان كمباع وربطه بالمشتري والتقييم.

### التواصل

- محادثات مرتبطة بالإعلان.
- محادثات عامة بين المستخدمين من خلال `conversations`.
- إرسال نصوص وصور، وعدد غير مقروء، قراءة، كتم، تثبيت، إغلاق وحذف.
- تحديثات فورية عبر Socket.IO.
- حظر المستخدمين.
- صندوق دعم منفصل للمستخدم والمدير مع سلة دعم.

### الطلبات والصفقات الآمنة

- إنشاء طلب شراء ودعم الدفع/الإيصال حسب التدفق الموجود.
- حالات الطلب وتحديثه من المشتري أو البائع أو الإدارة وفق الصلاحية.
- ربط الطلب بالإعلان والبائع والمشتري.
- محافظ ومعاملات وسحوبات وحسابات بنكية للمستخدم.
- وساطة مالية ومراقبة ونزاعات وسجلات escrow.
- عمولات البائع ورفع مستندات العمولة ودفعها.

### التحقق والثقة

- طلب توثيق البائع مع مستندات.
- موافقة أو رفض الطلب من الإدارة.
- تقييم البائع وتقييم الإعلان والتقييمات المعلقة.
- تقييمات المنصة العامة ورد الإدارة عليها.
- بلاغات الإعلانات والمستخدمين والتعليقات.
- سجلات تدقيق للعمليات الإدارية الحساسة.

### الاشتراكات والتمييز

- عرض الباقات العامة.
- إدارة الباقات من الإدارة.
- اشتراك البائع.
- طلب تمييز الإعلان ومراجعته.
- حساب مدة التمييز وانتهائها.

### الإحالات والتسويق

- ملف إحالة للمستخدم ورمز مشاركة.
- تسجيل الزيارات والعلاقات الإحالية.
- احتساب عمولات الإحالة وطلبات سحبها.
- لوحات الإدارة والتحليلات الخاصة بالإحالات.
- حملات وساطة، عضويات، أدلة، صفقات، شكاوى، مراجعات وإنجازات.

### الإشعارات

- إشعارات داخل التطبيق.
- إشعارات Push عبر Web Push.
- بريد إلكتروني في الأحداث المدعومة.
- تفضيلات مستقلة للرسائل والتعليقات والمفضلة وحالة الإعلان والطلبات والمحفظة ورسائل الإدارة.
- قراءة إشعار منفرد أو جميع الإشعارات وحذفها.

## 6. API الخلفية

العنوان الأساسي المحلي الافتراضي هو `http://localhost:5000/api`. التوثيق التفاعلي متاح على `http://localhost:5000/api-docs` عند تشغيل الخادم.

### مجموعات المسارات

| المجموعة | النطاق |
|---|---|
| `/api/auth` | التسجيل، الدخول، الجلسة، OTP، الملف، كلمة المرور، Push. |
| `/api/ads` و`/api/products` | الإعلانات/المنتجات، المفضلة المرتبطة، التعليقات والتمييز. |
| `/api/admin` و`/api/admin/ads` | الإحصاءات، إدارة الإعلانات والمستخدمين والسجلات وسلة المحذوفات. |
| `/api/categories` و`/api/category-attributes` و`/api/attributes` | شجرة الفئات والخصائص الديناميكية. |
| `/api/governorates` و`/api/cities` | بيانات الموقع الجغرافي. |
| `/api/tags` | الأوسمة والإعلانات المرتبطة بها. |
| `/api/sellers` | الملفات العامة وبلاغات البائعين. |
| `/api/favorites` و`/api/follows` و`/api/blocks` | المفضلة والمتابعة والحظر. |
| `/api/conversations` و`/api/chats` و`/api/messages` | الرسائل والمحادثات المرتبطة بالإعلانات. |
| `/api/notifications` و`/api/email` | الإشعارات وتحقق البريد. |
| `/api/orders` و`/api/purchase-requests` | الطلبات وإيصالات الشراء ومراجعتها. |
| `/api/wallets` و`/api/commissions` | الرصيد والمعاملات والسحب والعمولات. |
| `/api/bank-accounts` و`/api/user-bank-accounts` | حسابات المنصة وحسابات المستخدم. |
| `/api/plans` و`/api/admin/plans` | الباقات وإدارتها. |
| `/api/verification-requests` | توثيق البائعين. |
| `/api/reviews` و`/api/platform-reviews` | تقييمات البائعين والمنصة. |
| `/api/support` و`/api/admin-messages` | الدعم والمراسلة العامة. |
| `/api/brokerage` | الوساطة والحملات والصفقات والتحليلات. |
| `/api/referrals` و`/api/admin/referrals` | الإحالات والعمولات والسحب وإدارة السفراء. |
| `/api/admin/escrow` و`/api/admin/analytics` | مراقبة الوساطة والتحليلات الإدارية. |

### مسارات الصحة والملفات

- `GET /api/health`: فحص اتصال قاعدة البيانات.
- `GET /api/health/liveness`: فحص أن العملية حية.
- `GET /api/health/readiness`: فحص الجاهزية وقراءة مستخدم من قاعدة البيانات.
- `GET /api/version`: رقم الإصدار ووقت الاستجابة.
- `/uploads`: الملفات المرفوعة مع placeholder للصور المفقودة ونسخ thumb/med عند توفرها.
- `/api-docs`: Swagger UI.

### نمط الاستجابة والأخطاء

تستخدم المسارات JSON في الاستجابات، وتعيد أخطاء HTTP مناسبة مع رسالة خطأ. يجب على العميل التعامل مع `401` بانتهاء الجلسة أو محاولة refresh، ومع `403` بانعدام الصلاحية، ومع `404` بعدم وجود المورد، ومع `429` بتجاوز الحد، ومع `500` بخطأ خادم.

## 7. نموذج البيانات

### الكيانات الأساسية

`User`, `Ad`, `Product`, `Category`, `CategoryAttribute`, `Attribute`, `AdAttributeValue`, `ListingAttributeValue`, `Tag`, `Governorate`, `City`.

### التواصل والثقة

`Conversation`, `ConversationMessage`, `Chat`, `Message`, `Notification`, `SupportConversation`, `SupportMessage`, `Block`, `Follow`, `Favorite`, `Comment`, `AdComment`, `Review`, `PlatformReview`, `AdReport`, `SellerReport`.

### المالية والطلبات

`Order`, `PurchaseRequest`, `Transaction`, `Wallet`, `Withdrawal`, `Commission`, `Bank`, `UserBankAccount`, `Dispute`, `AdminEscrowLog`, `Plan`.

### الوساطة والإحالات

`BrokerProfile`, `BrokerageConfig`, `BrokerageCampaign`, `BrokerageMembership`, `BrokerageDeal`, `BrokerageEvidence`, `BrokerageComplaint`, `BrokerageReview`, `BrokerageAchievement`, `BrokerageBadge`, `BrokerageAuditLog`, `ReferralProfile`, `ReferralRelationship`, `ReferralCommission`.

### التشغيل والتدقيق

`ActivityLog`, `AdminAuditLog`, `AdminMessage`, `AdminNotification`, `AdminPushNotification`, `DailyVisitor`, `AdView`, `PasswordResetRequest`, `OtpCode`, `SystemSettings`, `SoldListing`.

### حالات الإعلان

الحقل `status` في `Ad` يدعم `pending`, `approved`, `rejected`, `expired`, `sold`, `AVAILABLE`, `SOLD`, `archived`, `deleted`, `blocked`, `reported`. كما توجد حقول مستقلة للظهور والحذف والأرشفة والبيع والانتهاء والتمييز؛ لذلك يجب تعديل الحالة عبر الخدمات والمسارات المعتمدة بدل تحديثها مباشرة من الواجهة.

## 8. الأمان والامتثال التشغيلي

- CORS مع دعم النطاقات المحلية ونطاقات النشر.
- رؤوس أمان عبر middleware مخصص وHelmet ضمن إعدادات الأمان.
- تنظيف مدخلات MongoDB وXSS وHPP.
- Rate limiting عام مع حدود خاصة للمراسلات وبعض المسارات.
- رقم طلب لكل طلب وسجل HTTP عبر Morgan.
- JWT وrefresh token وملفات تعريف ارتباط.
- التحقق من الأدوار على مستوى المسار.
- التحقق من البيانات باستخدام Joi أو Zod حسب الوحدة.
- تخزين الصور والوثائق في مسار uploads، مع Sharp لمعالجة الصور.
- Sentry اختياري عبر `SENTRY_DSN`.

### ملاحظات إنتاجية مهمة

1. يجب تغيير `JWT_SECRET` الافتراضي وعدم استخدامه في الإنتاج.
2. يجب تقييد `allowedOrigins` بدل السماح الاحتياطي الحالي أثناء التطوير.
3. يجب مراجعة تفعيل CSRF وBot Detection قبل النشر العام؛ كلاهما معطل حاليًا في `backend/src/index.js`.
4. يجب حماية مجلد uploads ونسخ MongoDB احتياطيًا ومراقبة حجمهما.
5. يجب عدم تسجيل الرموز أو كلمات المرور أو محتوى الوثائق الحساسة.

## 9. التشغيل المحلي

### المتطلبات

- Node.js 18 أو أحدث.
- npm.
- MongoDB 6 أو Docker.

### باستخدام Docker

```powershell
docker compose up --build
```

يعمل MongoDB على `27017` والخادم على `5000`.

### بدون Docker

```powershell
cd backend
npm install
npm run dev
```

وفي طرفية أخرى:

```powershell
cd frontend
npm install
npm run dev
```

الواجهة الافتراضية على `http://localhost:5173`.

### متغيرات البيئة الأساسية

| المتغير | الاستخدام |
|---|---|
| `MONGODB_URI` | اتصال MongoDB. |
| `JWT_SECRET` | توقيع JWT. |
| `PORT` | منفذ الخادم. |
| `FRONTEND_ORIGIN` | مصادر الواجهة المسموح بها. |
| `VITE_API_URL` | عنوان API للواجهة. |
| `VITE_SOCKET_URL` | عنوان Socket.IO. |
| `VAPID_PUBLIC_KEY` و`VAPID_PRIVATE_KEY` و`VAPID_EMAIL` | Web Push. |
| `SENTRY_DSN` | تتبع أخطاء Sentry اختياريًا. |
| إعدادات البريد | إرسال OTP والبريد التنبيهي. |

## 10. الاختبارات والجودة

أوامر المشروع الحالية:

```powershell
cd backend
npm test

cd ..\frontend
npm test
npm run build
```

ينبغي أن تغطي الاختبارات على الأقل:

- التسجيل والدخول والتجديد وتحديد الأدوار.
- عدم ظهور إعلان pending للعامة.
- دورة اعتماد ورفض وبيع وأرشفة الإعلان.
- صلاحيات المدير والبائع والمشتري.
- إنشاء الطلب وتحديثه والوساطة والسحب.
- الرسائل وعدد غير المقروء والإشعارات.
- الحذف المنطقي والاستعادة.
- الإحالات والعمولات.

## 11. النسخ الاحتياطي والتشغيل

- إجراء نسخة احتياطية دورية من MongoDB قبل أي migration أو backfill.
- الاحتفاظ بنسخة من مجلد `uploads` بالتزامن مع قاعدة البيانات.
- مراقبة `/api/health/readiness` من خدمة الاستضافة.
- استخدام `/api/health/liveness` لإعادة تشغيل الحاوية عند توقف العملية.
- مراجعة `backend/logs` وسجلات الاستضافة دوريًا.
- تشغيل scripts الموجودة في `backend` فقط بعد مراجعة نطاقها ونسخ البيانات.

## 12. حدود الوثيقة والفجوات الحالية

- بعض صفحات الإدارة القديمة والجديدة موجودة معًا، مثل `AdminDashboard` و`AdminDashboardNew`؛ المسار الحالي يستخدم `AdminDashboard`.
- أسماء `Product` و`Ad` مستخدمة بالتوازي، بينما المسارات العامة الأساسية تعتمد الإعلانات؛ يجب توحيد التسمية تدريجيًا.
- README الحالي يصف نسخة أصغر من النظام ولا يعكس جميع الأنظمة المدرجة هنا.
- تفاصيل payload لكل endpoint يجب أن تظل في Swagger وتعليقات المسارات، ويمكن إضافة ملف OpenAPI ثابت لاحقًا.
- إعدادات البريد والدفع الفعلي تختلف حسب بيئة التشغيل ويجب تثبيتها في ملف إعداد إنتاجي موثق.
- يلزم إضافة اختبارات تكامل API واختبارات صلاحيات شاملة قبل اعتبار النظام جاهزًا لإصدار إنتاجي كبير.

## 13. مرجع الصيانة السريع

عند إضافة ميزة جديدة:

1. أضف أو حدّث نموذج Mongoose.
2. ضع منطق المجال في service أو engine عند الحاجة.
3. أضف مسار API مع التحقق والصلاحية والتدقيق للعمليات الحساسة.
4. أضف عميل API في `frontend/src/api`.
5. أضف الصفحة والمسار والحماية في `frontend/src/App.jsx`.
6. حدّث هذه الوثيقة وSwagger.
7. أضف اختبارًا لتدفق النجاح وفشل الصلاحية والحالة غير الصحيحة.
8. شغّل اختبارات الخادم والواجهة وbuild الواجهة.

## 14. ملفات البداية المهمة

- `frontend/src/App.jsx`: مسارات الواجهة وحمايتها.
- `frontend/src/layouts/MainLayout.jsx`: الهيكل العام للواجهة.
- `frontend/src/pages/AdminLayout.jsx`: قائمة لوحة الإدارة وتحديثات الدعم.
- `frontend/src/store/AuthContext.jsx`: دورة الجلسة والمصادقة.
- `backend/src/index.js`: تهيئة الخادم والـ middleware وتركيب المسارات.
- `backend/src/routes/`: جميع مسارات API.
- `backend/src/models/`: نماذج MongoDB.
- `docker-compose.yml`: تشغيل MongoDB والخادم محليًا.
- `frontend/package.json` و`backend/package.json`: الأوامر والاعتمادات.
